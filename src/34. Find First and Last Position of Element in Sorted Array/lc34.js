/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[]}
 */
var searchRange = function (nums, target) {
 let output = []
	if( output.length > 2 ){
		return output
	}
	else{
		if( target > nums[ Math.floor( nums.length / 2)]){
			searchRange( nums.slice( Math.floor( nums.length / 2 ))
		}
	}
}